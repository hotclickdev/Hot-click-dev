name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # Cada 10 minutos — Render duerme tras 15 min sin tráfico
  workflow_dispatch:         # Permite ejecutarlo manualmente desde GitHub

# E9 pager (health-pager.yml) y D12 (real-health.yml) están pausados 2026-09-14
# (solo workflow_dispatch). Este workflow solo pinea para que Render no duerma.

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Ping health endpoint
        run: |
          set -euo pipefail
          URL="${HEALTH_URL:-https://hot-click-dev.onrender.com/api/health}"
          STATUS=$(curl -sS -o /dev/null -w "%{http_code}" --max-time 20 "$URL" || echo 000)
          echo "Status: $STATUS url=$URL"
          if [ "$STATUS" != "200" ]; then
            echo "Warning: health check returned $STATUS"
          fi
        env:
          HEALTH_URL: ${{ secrets.HEALTH_URL }}

